// Create function func with arguments
// the task condition is in the Readme file

import 'dart:math';
num func(num a,num b){
    return (pow((a+b/2),2))*(b/2);
}
void main(){

    print(func(2,4));
}

